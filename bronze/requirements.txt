# Runtime. openpyxl is the only dependency - the pipeline reads xlsx and
# writes CSV with the standard library. pandas would add ~50MB and a numpy
# ABI constraint to save nothing here: there is no melt, no arithmetic, and
# every value lands as text.
openpyxl>=3.1

# Tests use the standard library only (unittest), so nothing extra is needed:
#   python -m unittest discover -s tests
